// para criar um commit novo com comentário? siga o seguinte:

git add .
git commit -m "Fix: modificação no código"
git push

// para só atualizar um commit novo com comentário siga o seguinte:

git add .
git commit --amend --no-edit
git push --force

